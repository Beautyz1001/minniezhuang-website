# AGENTS.md — 个人作品集网站协作规则

> 以下内容按优先级从高到低排列。上面的规则优先于下面的规则。

---

## 0. 最高优先级：硬性红线

**视觉局部修改协议**
用户提出指向明确的局部视觉改动时（如"给门洞增加光影"）：
- 只改用户点名的对象及实现该效果必需的属性，其余一律**冻结**（背景、灯光颜色、字体、布局、镜头、动画节奏、素材、其他组件）。
- 不得以"更协调""提升氛围""顺便优化"为由碰冻结项。
- 若必须动冻结项：先停止，说明要动什么、为什么不可避免、会造成什么变化，等用户同意再改。

开工前用不超过4行复述：目标对象 / 允许改动 / 保持不变 / 验收结果。

**每个任务开工前必须先反向确认（2026-09-09 用户要求）**
不管指令看起来多明确，**收到任务后不许直接动手**，先做两件事，然后停下来等用户回话：

1. **确认方向**：把这次要达成的目标用一句话说清楚，并提出我判断不确定、有歧义、
   或者有多种做法的点，请用户拍板。没有疑问也要明确说「我这边没有疑问，是否开始」。
2. **说自己的理解**：复述我理解的——改哪个对象、允许动什么、什么冻结、做完什么样算过。

- ⚠ 这条**覆盖**旧的「指令已足够明确时不要反问」，那句作废。
- 用户回「开始 / 对 / 就这样做」之后才动手；用户直接补充或纠正的，按补充的来。
- 只读类任务（用户说"看一下""读一下""检查""分析"）同样先说理解，但可以说完直接读，
  不必等回话——因为不改东西。**只要会改文件，一律先停。**

**卡住了才回头，没卡住就一路做完（2026-09-09 用户改定，取代原来的"5 分钟必须汇报"）**
**不要用时间当汇报的触发条件。**任务做得久不是打断用户的理由——闷头做很久但一路顺，
就一路做完再汇报。**该停的是遇到"卡点"，不是"到点"。**

什么算卡点（遇到就立刻停下来说清楚，等用户定了再继续）：
- **方向要变**：做下去发现原来的方案不成立、或者要改动用户没点名的东西。
- **量出来的结果和用户的说法对不上**：这时候多半是我测错了或理解错了，
  别自己挑一个解释就往下做。
- **同一个问题连着两次没解决**：说明思路错了，不是手法问题。换第三种做法之前先说。
- **要在没有依据的地方拍数**：参考站里查无此数、又必须填一个值时，先问。
- **动作卡死不动**：工具连不上、服务器起不来、命令没反应，先说，不要闷头重试。

⚠ 停下来的时候要给的是：**卡在什么上 / 我判断是什么原因 / 有哪几条路 / 我建议哪条**，
不是光说一句"卡住了"。
⚠ 反过来也成立：**没到卡点就不要中途邀功式汇报**，做完再一次说清楚。

**全站保护规则**
除非用户明确要求，不改：导航结构、字体体系、页面路由、其他页面、全局设计系统、已完成视觉效果。不随意重构文件结构、删除已有功能、替换已有实现方案、重命名文件。

**调试诚实原则（新增）**
调试或修复问题时：
- 禁止用绕过问题的方式让代码"看起来能跑"：不允许 mock/硬编码掩盖逻辑错误、删除报错功能来消除报错、用 try-catch 吞掉异常不查根因、为让测试通过而改测试本身（除非测试确实写错并说明原因）。
- 修复前先说明：根本原因是什么、打算怎么修、为什么这是对的方案而不是最省事的方案。
- 排查后发现问题复杂需要更大改动，直接告知用户，不要用简化方案敷衍。

**已否决方案禁止复用（新增）**
用户明确否定过的方案、写法、思路（说过"不对""效果不好""不要这样做"的），视为永久拉黑，不是本次任务内有效，而是**整个项目周期内有效**，记录在 `PITFALLS.md` 中（不存在则创建）。

维护规则：
- 用户否定一个方案时，立刻在 `PITFALLS.md` 追加一条，格式：
  ```
  ## [日期] 涉及区域（如"门洞光影"）
  - 尝试方案：简述做了什么
  - 用户反馈：用户原话或概括
  - 判断：不要再用的原因（如果知道）
  ```
- **每次修改前先读 `PITFALLS.md`**，检查这次打算用的方案是否和已记录的某条本质同类（哪怕换了变量名、实现细节不同，逻辑一样也算）。如果是，禁止使用，换根本不同的思路。
- 想不出新思路、担心又撞回老路子时，先说明"这个方案和 PITFALLS.md 里第几条思路类似，原因是___，需要确认是否可以采用，或提示应该往哪个方向想"，不要自己揣测直接改。
- 用户说"这个问题说过N遍了"，说明严重违反本条：先在 `PITFALLS.md` 里定位对应记录并复述，而不是先道歉。
- `PITFALLS.md` 只增不删，条目多了不用整理归纳，保持原始记录以便追溯。

**任务收尾清理（新增）**
完成任务前检查并清理本次产生的临时/调试文件（调试日志、诊断目录、临时截图、自建的进度记录等，以 `.` 开头的隐藏临时目录同样适用，除非是项目本身需要的配置）。删除前先列清单确认属于自己创建的临时产物，再执行。任务结束时目录里只应保留与最终改动相关的文件。`PITFALLS.md` 和 `HANDOFF.md` 是长期文档，不属于清理对象。

**Works 预览与验收（2026-09-12 用户要求）**
- Works 预览只使用用户已指定并已启动的那一个本地预览服务；除非用户明确要求，不得自行启动第二个服务、替换端口或另开预览方案。
- Codex 每完成一版，先直接交给用户在该预览页目视验收。用户的眼睛是验收标准；Codex 不需要、也不要为了自行验收而创建或保留截图、截图目录或截图文件。
- 本条只约束 Codex 的验收与临时产物；用户明确要求截图时再按要求提供。Claude 的截图流程不受本条影响。

---

## 1. 修改流程

用户说"讨论/检查/分析"→ 只读不改。
用户说"修改/实现/修复"→ 执行，但：

**修改前**说明：会读哪些文件、会改哪些文件、目标是什么。
**修改时**：只改相关范围，不顺便优化其他区域，不改架构。
**完成后**说明：改了什么、没改什么、如何查看效果。

**理解审美反馈**："不对/不好看/蠢/感觉不对" = 上次实现偏离要求，不是授权重做整页。
- 先指出具体误解了哪一项，只撤回/调整相关项，不借机重做未被批评的部分。
- 用户说"不要改X" → X 加入本次冻结清单。
- 反馈优先归类到：颜色 / 大小 / 位置 / 动画 / 节奏 / 氛围。

---

## 1b. 命名操作：`取景`

**用户说「给 XXX 调取景」+ 给一张参考图，就是要执行这套流程。**

含义：**框一个像素不动，只改框里露出图片的哪一块、露多大**——相当于在相框后面挪照片、
再把照片放大一点。不换素材文件、不改框的尺寸和位置、不改版式、不碰其他卡。

**执行步骤（顺序不能变，第 2 步不许跳）**

1. **读现状**：起 `dev-server.py`，实测这一格的框宽高、素材的原始宽高、当前的 `object-fit` /
   `height` / `object-position`。
2. **量参考图，不许目测。** 把参考图和素材原件都降采样成灰度，做零均值归一化相关匹配，
   搜出参考图对应素材上的哪一块窗口（输出：可见窗口的 x/y 范围、占原图宽高的比例、
   四边各裁掉多少）。相关系数低于 0.9 说明匹配不可信，停下来问用户。
   ⚠ 这一步直接对应 PITFALLS [2026-09-05]「衔接不顺先量不要先调」——
   一次匹配就能拿到准确的数，凭感觉调十轮也未必对得上。
3. **反算三个数**（只有这三个旋钮，都写在那一格自己的选择器下，不许改公共规则）：
   - `height: N%` —— 决定放大倍数。纵向可见比例 = 1/N，横向跟着一起变，
     两个方向是同一个数控制的，不能各调各的。
   - `object-position: X% 50%` —— 决定横向对准哪一块。
     ⚠ 图由高度驱动 cover 时，纵向那个百分比是**废的**，别在它上面浪费时间。
   - `position: relative; top: -Y%` —— 纵向偏移只能靠它。
     ⚠ **绝不用 `transform`**：GSAP 每帧重写 transform，静态值会被算成起始值、位移翻倍。
4. **验算滚动余量**。图放大后上下藏起来的那两截就是滑动余量，
   **较小的那一侧决定这一格的滑动幅度上限**。默认 ±6% 放不下时，
   在这张 img 上写 `data-slide="N"`（JS 会读，不写就是 6），其余卡不受影响。
   幅度 = 较小一侧余量 ÷ 图片显示高度，再留 1px 安全量。
   ⚠ 必须实测：把 transform 设成 ±N% 逐个量上下缝隙，两边都 ≥0 才算过。
5. **截图交付**：这一格截一张，**再回页面顶部截一张**（PITFALLS [2026-09-07]），
   控制台确认无报错，并报告「另外几条卡的图片参数没变」的实测结果。

**硬性约束**

- 只改用户点名的那一格。其余卡、框尺寸、版式、文字栏、素材文件一律冻结。
- **参考图就是规格**（PITFALLS [2026-09-05]）。量出来是什么就是什么，
  不许因为"这样更好看"自己挪一点。如果按参考图会牺牲别的东西（例如滑动幅度被迫变小），
  **照参考图做，然后把代价讲清楚交给用户选**，不要自己替他折中。
- 分辨率验算：放大后图在屏幕上的显示宽度 ×2 不得超过素材原始宽度，超了要提醒用户换素材。
- 这条规则会连窄屏一起生效。桌面端确认后单独看一眼窄屏，不为窄屏另写一套。

---

## 2. Token 节省

- 不主动扫描整个项目，优先读用户指定文件。
- CSS 问题读 CSS，JS 问题读相关 JS。
- 不输出完整文件内容，不重复解释已确定的信息。
- 跨页面修改范围不明确时，先问是否要一起改。

---

## 3. 项目背景

**定位**：个人作品集网站（设计/音乐/写作），用户是设计师非程序员，代码靠 AI 协作生成。用户负责视觉方向和最终决策，AI 负责实现和维护稳定，不主动改变设计方向，不用通用模板替代设计意图。

**结构**：
- 正式工作区：`site/`。当前只有 `site/home/` 在制作；`site/works/`、`site/break/`、`site/about/` 留待各自独立开发；`site/shared/` 仅存放未来真正跨页复用的代码与素材。
- Home 开工前先读 `site/home/_internal/HANDOFF.md`；正式首页文件是 `site/home/index.html`。
- 旧版四页及其 `assets/`、`css/`、`js/` 已冻结在 `archive/v1-static-site/`，除非用户明确要求回看或修复旧版，不读取、不修改。

**技术栈**：HTML / CSS / JS / Three.js·WebGL。不主动引入 React、Vue、新框架、构建工具、新依赖。

**设计方向**：黑紫暗色、高级、克制、沉浸、电影感，强调光影和空间感；参考美术馆级体验和 James Turrell 式光影。优先还原视觉感受而非套用常见网页设计。

**Three.js**：必须通过本地服务器运行，不用 `file://` 直接打开。改动时优先保持已有 scene / camera / renderer / animation loop / asset loading，不无理由重写架构或换资源流程。

**本地预览**：Home 工作区在 `site/home/` 运行 `python dev-server.py`，访问 `http://localhost:8788/`。

**协作方式**：用户主要用图片、视觉参考、描述表达需求。解释时避免术语，关注最终视觉效果，用直观描述。

---

## 4. 近期交付大纲（约一周内）

**目标**：约一周内完成可上线版本。设计质量优先，但不得把新方向无限叠加到已完成的页面；若范围与期限冲突，先砍非必要动效或次要内容，不压缩核心页面的完成度。

**Homepage 的定位（2026-09-07 用户明确，以此为准，高于下面的分页职责）**：

Homepage 是一条**视觉走廊**，作用是给用户的**视觉能力和工作背书**。它**只有四个部分**：
`opening` / `works` / `break` / `contact`。不要再往里加第五个部分。

Homepage 本身是**导航页，不是具体 section 的容器**。页面里的 works 和 break 都只是概览；
导航点击是进入各自的**总览页**再展开，**不做页内锚点跳转**。所以不要给这些段落设计
"点导航跳到本页某一段"的行为，也不要因为"锚点跳错了"去改结构。

**Works 与 Break 的归属判定（2026-09-15 用户改定，取代旧版）**：

- **Works** = 腾讯作品、网易作品、AI 相关作品、`BUBBLE DELIVERY`、VR 项目（`BELONGINGS`）。
- **Break** = 自己做的游戏 demo（含原先算 Works 的 `BUBBLE DELIVERY` 之外的所有 demo）、
  场景设计（`ALONE IN KYOTO`，即电影院那一段）。

⚠ **VR 项目 (`BELONGINGS`) 已从 Break 改划入 Works**（旧版判定标准写反了，已作废，
不要再按"是不是接的活"去把 VR 推回 Break）。**所有 demo 类项目归 Break。**
**不要再用"影像/音乐/摄影/实验"这类媒介清单去判定归属**，那也是更早的旧定义，同样作废。

**四页职责与内容归属**：

- **Home**：负责第一印象、空间叙事和少数精选项目的入口。保留现有长滚动，不为效率重做整种表达；效率由导航和明确的 `VIEW ALL WORKS` 快捷入口解决。Home 卡片只展示预览，点击进入项目详情。
- **Works**：负责高效率、完整地展示所有**工作类**项目。它是这类项目内容的唯一归属，未来的详情页放在 `site/works/projects/<project-slug>/`；Home 和 Works 的同一项目必须跳到同一份详情页，禁止维护两套项目内容。
- **Break**：负责展示工作之外的加分项（见上面的归属判定）。它和 Works 的区别是**内容性质**，不是展示规格——Break 里的项目一样可以有完整的项目页和大尺寸视觉。就本周排期而言，Break 页优先做素材策展、排序与视觉判断，不新开高复杂度 3D 工程。
- **About**：负责介绍用户本人、经历/能力、简历和联系入口。完成标准是访客能快速理解“用户是谁、做什么、如何联系”。

**本周节奏**：

- Home 收尾与冻结：1 天。只处理阻塞性问题；不再开新视觉方向。
- Works：2.5 天。先在第 2 天完成参考、项目清单、封面/标签/内容盘点，再进入实现。
- Break：1.5 天。先确定参考、类别和素材，再实现。
- About：1 天。
- 全站联调与缓冲：1 天。检查导航、链接、文案、素材缺失、桌面与移动端。

**工作区阅读边界**：

- 做 Home：只读 `site/home/` 和必要的全站规则；不读 `archive/`、未来的 Works/Break/About 工作区。
- 做 Works/Break/About：只读各自页面工作区和必要的 `site/shared/`；不读 Home 的 Three.js、雾和滚动实现。
- `archive/v1-static-site/` 永远只用于回看/回退，不作为新页面的代码来源。
- 只有两页以上确实共享的代码、素材或内容索引，才能放进 `site/shared/`；不要为了“看起来整齐”过早抽象。

**用户需要先提供的内容**：

- Works：项目全清单、优先级、已有图/视频/文字。
- Break：想展示的素材类别、可用素材和参考。
- About：自我介绍、经历、简历文件或链接、联系信息与照片决定。

---

## 5. 阶段结束

完成一个目标后报告：修改了什么文件 / 完成内容 / 未修改内容 / 下一步建议。然后等待指令。

**同步更新 HANDOFF.md（新增）**
若当前任务在有 `HANDOFF.md` 的页面工作区下（如 `site/home/`），阶段结束汇报的同时，把内容同步写入该文件的「当前状态」区块：

- `HANDOFF.md` 分两块：
  1. **长期背景**（实验目标、核心约束、关键文件分工）——几乎不变，不主动改动，除非实验方向本质调整。
  2. **当前状态**（本次做到哪一步、上一步效果如何、未完成事项、下一步建议、待用户确认的问题）——**每次整体覆写，不追加**，保持简短，避免新对话框打开时要读一堆过时信息。
- 换新对话框开始任务前，先读 `HANDOFF.md` 的「当前状态」了解进度，不用用户手动重复说明。
